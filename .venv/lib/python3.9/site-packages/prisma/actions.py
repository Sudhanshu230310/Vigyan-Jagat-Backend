# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class WholesaleQuoteActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.WholesaleQuote]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await WholesaleQuote.prisma().query_raw(
            'SELECT * FROM WholesaleQuote WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.WholesaleQuote
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await WholesaleQuote.prisma().query_first(
            'SELECT * FROM WholesaleQuote WHERE name = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.WholesaleQuoteCreateInput,
        include: Optional[types.WholesaleQuoteInclude] = None
    ) -> _PrismaModelT:
        """Create a new WholesaleQuote record.

        Parameters
        ----------
        data
            WholesaleQuote record data
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model

        Returns
        -------
        prisma.models.WholesaleQuote
            The created WholesaleQuote record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a WholesaleQuote record from just the required fields
        wholesalequote = await WholesaleQuote.prisma().create(
            data={
                # data to create a WholesaleQuote record
                'name': 'ggciceaie',
                'email': 'bbehjachib',
                'phone': 'cadfabfehe',
                'organization': 'dgiiaaijj',
                'quantity': 'bfaiacjjfc',
                'message': 'eigcfgbif',
                'product_name': 'bagcfbhiig',
                'subcategory_name': 'cghideieh',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.WholesaleQuoteCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple WholesaleQuote records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of WholesaleQuote record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await WholesaleQuote.prisma().create_many(
            data=[
                {
                    # data to create a WholesaleQuote record
                    'name': 'biabhbdai',
                    'email': 'idghgaicb',
                    'phone': 'fjfddhigg',
                    'organization': 'hjaecfifb',
                    'quantity': 'cbbbjbfcii',
                    'message': 'bbejhfidcb',
                    'product_name': 'bgeecijdgg',
                    'subcategory_name': 'bdiicjafbj',
                },
                {
                    # data to create a WholesaleQuote record
                    'name': 'bgehebiafc',
                    'email': 'bghffegacj',
                    'phone': 'bhghchehcc',
                    'organization': 'dcgchcbbf',
                    'quantity': 'bdedcabahc',
                    'message': 'ghfhiafcb',
                    'product_name': 'heejgedji',
                    'subcategory_name': 'bjgjgibgbf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.WholesaleQuoteWhereUniqueInput,
        include: Optional[types.WholesaleQuoteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single WholesaleQuote record.

        Parameters
        ----------
        where
            WholesaleQuote filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model

        Returns
        -------
        prisma.models.WholesaleQuote
            The deleted WholesaleQuote record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        wholesalequote = await WholesaleQuote.prisma().delete(
            where={
                'id': 1116175964,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.WholesaleQuoteWhereUniqueInput,
        include: Optional[types.WholesaleQuoteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique WholesaleQuote record.

        Parameters
        ----------
        where
            WholesaleQuote filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model

        Returns
        -------
        prisma.models.WholesaleQuote
            The found WholesaleQuote record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        wholesalequote = await WholesaleQuote.prisma().find_unique(
            where={
                'id': 861472101,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.WholesaleQuoteWhereUniqueInput,
        include: Optional[types.WholesaleQuoteInclude] = None
    ) -> _PrismaModelT:
        """Find a unique WholesaleQuote record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            WholesaleQuote filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model

        Returns
        -------
        prisma.models.WholesaleQuote
            The found WholesaleQuote record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        wholesalequote = await WholesaleQuote.prisma().find_unique_or_raise(
            where={
                'id': 1303003706,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WholesaleQuoteWhereInput] = None,
        cursor: Optional[types.WholesaleQuoteWhereUniqueInput] = None,
        include: Optional[types.WholesaleQuoteInclude] = None,
        order: Optional[Union[types.WholesaleQuoteOrderByInput, List[types.WholesaleQuoteOrderByInput]]] = None,
        distinct: Optional[List[types.WholesaleQuoteScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple WholesaleQuote records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of WholesaleQuote records returned
        skip
            Ignore the first N results
        where
            WholesaleQuote filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model
        order
            Order the returned WholesaleQuote records by any field
        distinct
            Filter WholesaleQuote records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.WholesaleQuote]
            The list of all WholesaleQuote records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 WholesaleQuote records
        wholesalequotes = await WholesaleQuote.prisma().find_many(take=10)

        # find the first 5 WholesaleQuote records ordered by the email field
        wholesalequotes = await WholesaleQuote.prisma().find_many(
            take=5,
            order={
                'email': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.WholesaleQuoteWhereInput] = None,
        cursor: Optional[types.WholesaleQuoteWhereUniqueInput] = None,
        include: Optional[types.WholesaleQuoteInclude] = None,
        order: Optional[Union[types.WholesaleQuoteOrderByInput, List[types.WholesaleQuoteOrderByInput]]] = None,
        distinct: Optional[List[types.WholesaleQuoteScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single WholesaleQuote record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            WholesaleQuote filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model
        order
            Order the returned WholesaleQuote records by any field
        distinct
            Filter WholesaleQuote records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.WholesaleQuote
            The first WholesaleQuote record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second WholesaleQuote record ordered by the phone field
        wholesalequote = await WholesaleQuote.prisma().find_first(
            skip=1,
            order={
                'phone': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.WholesaleQuoteWhereInput] = None,
        cursor: Optional[types.WholesaleQuoteWhereUniqueInput] = None,
        include: Optional[types.WholesaleQuoteInclude] = None,
        order: Optional[Union[types.WholesaleQuoteOrderByInput, List[types.WholesaleQuoteOrderByInput]]] = None,
        distinct: Optional[List[types.WholesaleQuoteScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single WholesaleQuote record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            WholesaleQuote filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model
        order
            Order the returned WholesaleQuote records by any field
        distinct
            Filter WholesaleQuote records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.WholesaleQuote
            The first WholesaleQuote record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second WholesaleQuote record ordered by the organization field
        wholesalequote = await WholesaleQuote.prisma().find_first_or_raise(
            skip=1,
            order={
                'organization': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.WholesaleQuoteUpdateInput,
        where: types.WholesaleQuoteWhereUniqueInput,
        include: Optional[types.WholesaleQuoteInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single WholesaleQuote record.

        Parameters
        ----------
        data
            WholesaleQuote record data specifying what to update
        where
            WholesaleQuote filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model

        Returns
        -------
        prisma.models.WholesaleQuote
            The updated WholesaleQuote record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        wholesalequote = await WholesaleQuote.prisma().update(
            where={
                'id': 1686638315,
            },
            data={
                # data to update the WholesaleQuote record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.WholesaleQuoteWhereUniqueInput,
        data: types.WholesaleQuoteUpsertInput,
        include: Optional[types.WholesaleQuoteInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            WholesaleQuote filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned WholesaleQuote model

        Returns
        -------
        prisma.models.WholesaleQuote
            The created or updated WholesaleQuote record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        wholesalequote = await WholesaleQuote.prisma().upsert(
            where={
                'id': 2000430152,
            },
            data={
                'create': {
                    'id': 2000430152,
                    'name': 'bgehebiafc',
                    'email': 'bghffegacj',
                    'phone': 'bhghchehcc',
                    'organization': 'dcgchcbbf',
                    'quantity': 'bdedcabahc',
                    'message': 'ghfhiafcb',
                    'product_name': 'heejgedji',
                    'subcategory_name': 'bjgjgibgbf',
                },
                'update': {
                    'name': 'bgehebiafc',
                    'email': 'bghffegacj',
                    'phone': 'bhghchehcc',
                    'organization': 'dcgchcbbf',
                    'quantity': 'bdedcabahc',
                    'message': 'ghfhiafcb',
                    'product_name': 'heejgedji',
                    'subcategory_name': 'bjgjgibgbf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.WholesaleQuoteUpdateManyMutationInput,
        where: types.WholesaleQuoteWhereInput,
    ) -> int:
        """Update multiple WholesaleQuote records

        Parameters
        ----------
        data
            WholesaleQuote data to update the selected WholesaleQuote records to
        where
            Filter to select the WholesaleQuote records to update

        Returns
        -------
        int
            The total number of WholesaleQuote records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all WholesaleQuote records
        total = await WholesaleQuote.prisma().update_many(
            data={
                'quantity': 'bigibebcib'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WholesaleQuoteWhereInput] = None,
        cursor: Optional[types.WholesaleQuoteWhereUniqueInput] = None,
    ) -> int:
        """Count the number of WholesaleQuote records present in the database

        Parameters
        ----------
        select
            Select the WholesaleQuote fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            WholesaleQuote filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.WholesaleQuoteCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await WholesaleQuote.prisma().count()

        # results: prisma.types.WholesaleQuoteCountAggregateOutput
        results = await WholesaleQuote.prisma().count(
            select={
                '_all': True,
                'message': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.WholesaleQuoteCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WholesaleQuoteWhereInput] = None,
        cursor: Optional[types.WholesaleQuoteWhereUniqueInput] = None,
    ) -> types.WholesaleQuoteCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.WholesaleQuoteCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.WholesaleQuoteWhereInput] = None,
        cursor: Optional[types.WholesaleQuoteWhereUniqueInput] = None,
    ) -> Union[int, types.WholesaleQuoteCountAggregateOutput]:
        """Count the number of WholesaleQuote records present in the database

        Parameters
        ----------
        select
            Select the WholesaleQuote fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            WholesaleQuote filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.WholesaleQuoteCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await WholesaleQuote.prisma().count()

        # results: prisma.types.WholesaleQuoteCountAggregateOutput
        results = await WholesaleQuote.prisma().count(
            select={
                '_all': True,
                'product_name': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.WholesaleQuoteCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.WholesaleQuoteWhereInput] = None
    ) -> int:
        """Delete multiple WholesaleQuote records.

        Parameters
        ----------
        where
            Optional WholesaleQuote filter to find the records to be deleted

        Returns
        -------
        int
            The total number of WholesaleQuote records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all WholesaleQuote records
        total = await WholesaleQuote.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.WholesaleQuoteScalarFieldKeys'],
        *,
        where: Optional['types.WholesaleQuoteWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.WholesaleQuoteAvgAggregateInput'] = None,
        sum: Optional['types.WholesaleQuoteSumAggregateInput'] = None,
        min: Optional['types.WholesaleQuoteMinAggregateInput'] = None,
        max: Optional['types.WholesaleQuoteMaxAggregateInput'] = None,
        having: Optional['types.WholesaleQuoteScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.WholesaleQuoteCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.WholesaleQuoteScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.WholesaleQuoteScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.WholesaleQuoteGroupByOutput']:
        """Group WholesaleQuote records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar WholesaleQuote fields to group records by
        where
            WholesaleQuote filter to select records
        take
            Limit the maximum number of WholesaleQuote records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.WholesaleQuoteGroupByOutput]
            A list of dictionaries representing the WholesaleQuote record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group WholesaleQuote records by subcategory_name values
        # and count how many records are in each group
        results = await WholesaleQuote.prisma().group_by(
            ['subcategory_name'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models